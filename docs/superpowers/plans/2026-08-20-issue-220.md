# Issue #220: iOS 위젯 디자인 브릿지 (WidgetUpdateModule) — 구현 계획

**Goal:** `ios/WidgetUpdateModule.swift`의 `onWidgetDesignUpdated`/`onQtWidgetDesignUpdated`를 현재 스텁(JSON 문자열을 그대로 App Group `UserDefaults`에 저장)에서, Android `WidgetDesignPrefsDataSource`와 동등한 실제 구현으로 교체한다: 배경이 갤러리 이미지(`type: "gallery"`)면 원본 URI를 디코드해 다운샘플링·JPEG 압축 후 App Group 컨테이너 파일로 저장하고, 디자인 JSON의 `background.value`를 그 영구 경로로 치환해 저장한다. 렌더링 반영(#221)은 이번 스코프에 포함하지 않는다.

**Non-goals**: `MeditationBlossomWidget.swift`가 저장된 디자인을 실제로 읽어 렌더링하는 작업(#221, #220에 의존). `WidgetUpdateModule.mm`/TurboModule spec 변경 — 메서드 시그니처(`(String) -> Promise<Bool>`)가 그대로이므로 브릿지 계약은 변경 없음.

**Architecture 결정**: Android가 `WidgetDesignPrefsDataSource`(다운샘플링 2-pass + JPEG 압축 + SharedPreferences)로 구현한 것과 동일한 역할을, iOS는 `ImageIO`(`CGImageSourceCreateThumbnailAtIndex`)로 구현한다 — Android의 수동 `inSampleSize` 2-pass 디코딩과 달리 iOS는 `kCGImageSourceThumbnailMaxPixelSize` 옵션이 메모리 효율적 다운샘플링을 API 레벨에서 제공하므로 이를 그대로 사용한다(재구현 불필요). 핵심 로직(JSON 파싱 → 갤러리면 이미지 처리 → 경로 치환 → 재인코딩)은 App Group 접근에 의존하지 않는 순수 함수로 분리한다(`containerURL`을 파라미터로 주입) — Android는 Robolectric으로 `Context`를 모킹해 순수 로직을 테스트하지만, iOS 테스트 타깃은 App Group 엔타이틀먼트가 없어 `FileManager.containerURL(forSecurityApplicationGroupIdentifier:)`가 테스트 환경에서 `nil`을 반환한다 — 컨테이너 URL을 주입 가능하게 만들어야 실제 App Group 없이도 단위 테스트가 가능하다.

**Tech Stack:** Swift, ImageIO/UniformTypeIdentifiers(다운샘플링·JPEG 인코딩), XCTest(`meditation_blossomTests`, 기존에 실제 테스트 파일이 없는 빈 타깃 — 이번에 처음 채움).

**테스트 정책:** `meditation_blossomTests`에 `WidgetDesignPersistenceTests.swift` 신규 추가 — color 디자인 패스스루, gallery 디자인 다운샘플링+경로 치환, 잘못된 JSON에서 에러 throw, 3가지 순수 로직 케이스를 임시 디렉터리(App Group 아님)로 검증. 파일을 `project.pbxproj`의 `meditation_blossomTests` 타깃 Sources 빌드 페이즈에 `xcodeproj` gem으로 프로그래밍적으로 추가한다(Xcode GUI 없이 CLI 환경).

**빌드 검증**: 이 작업 환경은 macOS + Xcode 26.3이 설치되어 있어 `xcodebuild`로 실제 컴파일/테스트 실행이 가능하다(이전 이슈들의 "Windows라 iOS 컴파일 미검증" 제약이 이번엔 없음).

**커밋 정책:** `feature/issue-220` 브랜치, 커밋 메시지 `[ISSUE-220] type: 설명`.

---

## Tasks

### Task 1: 디자인 JSON 스키마(Codable) + 순수 영속화 로직 추가

**File:** `ios/WidgetUpdateModule.swift`

파일 상단에 `import ImageIO`, `import UniformTypeIdentifiers` 추가(다운샘플링/JPEG 인코딩에 필요, 기존 `import Foundation`/`import WidgetKit`에 추가).

`WidgetUpdateModuleImpl` 클래스 밖, 파일 최상위에 `internal` 접근수준(기본값, `private` 아님 — 테스트 타깃이 `@testable import`로 접근해야 함)의 새 enum 네임스페이스 추가:

```swift
// src/types/WidgetDesign.ts와 1:1 대응하는 브릿지 전송 타입.
// App Group 접근(FileManager containerURL)에 의존하지 않는 순수 함수로 분리해
// 테스트 타깃(App Group 엔타이틀먼트 없음)에서도 검증 가능하게 한다.
enum WidgetDesignPersistence {
  enum PersistenceError: Error {
    case invalidDesignJSON
    case imageDecodeFailed
    case imageWriteFailed
  }

  struct ImageTransform: Codable {
    let zoom: Double
    let focalX: Double
    let focalY: Double
  }

  struct TextDesign: Codable {
    let align: String
    let color: String
    let size: Int
    let weight: String
  }

  struct BackgroundDesign: Codable {
    let type: String
    var value: String
    let imageTransform: ImageTransform?
  }

  struct Design: Codable {
    let text: TextDesign
    var background: BackgroundDesign
  }

  static let maxBackgroundDimension: CGFloat = 1024
  static let backgroundJPEGQuality: CGFloat = 0.85

  // designData(JS WidgetDesign JSON)를 파싱해 background.type이 "gallery"면 이미지를
  // containerURL/backgroundFileName 경로로 다운샘플링·압축 저장하고, 그 영구 경로로
  // background.value를 치환한 JSON을 반환한다. "color"면 파싱↔재인코딩만 하고 그대로 반환.
  static func persist(designData: String, containerURL: URL, backgroundFileName: String) throws -> String {
    guard let data = designData.data(using: .utf8) else { throw PersistenceError.invalidDesignJSON }
    var design: Design
    do {
      design = try JSONDecoder().decode(Design.self, from: data)
    } catch {
      throw PersistenceError.invalidDesignJSON
    }

    if design.background.type == "gallery" {
      let destinationURL = containerURL.appendingPathComponent(backgroundFileName)
      try downsampleAndStore(sourcePath: design.background.value, to: destinationURL)
      design.background.value = destinationURL.path
    }

    guard let encoded = try? JSONEncoder().encode(design),
          let jsonString = String(data: encoded, encoding: .utf8) else {
      throw PersistenceError.invalidDesignJSON
    }
    return jsonString
  }

  // ImageIO의 썸네일 생성 옵션이 2-pass 다운샘플링을 API 레벨에서 제공하므로(Android처럼
  // inSampleSize를 직접 계산할 필요 없음), 원본을 통째로 메모리에 올리지 않고 바로 축소본을 얻는다.
  private static func downsampleAndStore(sourcePath: String, to destinationURL: URL) throws {
    let sourceURL = resolveFileURL(sourcePath)
    let thumbnailOptions: [CFString: Any] = [
      kCGImageSourceCreateThumbnailFromImageAlways: true,
      kCGImageSourceThumbnailMaxPixelSize: maxBackgroundDimension,
      kCGImageSourceCreateThumbnailWithTransform: true, // EXIF 회전 반영
    ]
    guard let imageSource = CGImageSourceCreateWithURL(sourceURL as CFURL, nil),
          let thumbnail = CGImageSourceCreateThumbnailAtIndex(imageSource, 0, thumbnailOptions as CFDictionary) else {
      throw PersistenceError.imageDecodeFailed
    }

    guard let destination = CGImageDestinationCreateWithURL(destinationURL as CFURL, UTType.jpeg.identifier as CFString, 1, nil) else {
      throw PersistenceError.imageWriteFailed
    }
    let writeOptions: [CFString: Any] = [kCGImageDestinationLossyCompressionQuality: backgroundJPEGQuality]
    CGImageDestinationAddImage(destination, thumbnail, writeOptions as CFDictionary)
    guard CGImageDestinationFinalize(destination) else {
      throw PersistenceError.imageWriteFailed
    }
  }

  // react-native-image-picker(iOS)는 선택한 사진을 앱 임시 디렉터리로 복사해 file:// URI로
  // 돌려준다. 이미 한 번 영속화된 값(재편집 시 로드된 기존 디자인)은 스킴 없는 절대 경로이므로,
  // 스킴이 있으면 URL(string:)으로, 없으면 URL(fileURLWithPath:)로 해석한다.
  private static func resolveFileURL(_ path: String) -> URL {
    if let url = URL(string: path), url.scheme != nil {
      return url
    }
    return URL(fileURLWithPath: path)
  }
}
```

- [ ] 구현
- [ ] 커밋: `[ISSUE-220] feat: WidgetDesignPersistence 순수 영속화 로직 추가`

---

### Task 2: `onWidgetDesignUpdated`/`onQtWidgetDesignUpdated`를 실제 저장 로직으로 교체

**File:** `ios/WidgetUpdateModule.swift`

`WidgetUpdateModuleImpl` 내부:

1. `Constants`에 배경 이미지 파일명 2개 추가(Android `widget_design_background_${suffix}.jpg`와 동일 네이밍):
   ```swift
   static let widgetDesignBackgroundFileNameSermon = "widget_design_background_sermon.jpg"
   static let widgetDesignBackgroundFileNameQt = "widget_design_background_qt.jpg"
   ```
2. App Group 컨테이너 URL 헬퍼 추가(기존 `appGroupDefaults()`와 대칭):
   ```swift
   private static func appGroupContainerURL() -> URL? {
     FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: Constants.appGroupId)
   }
   ```
3. 이미지 처리가 가벼운 작업은 아니므로, 기존 `resolveBibleReferences`의 `dbQueue` 패턴과 동일하게 전용 직렬 큐 추가:
   ```swift
   private static let designQueue = DispatchQueue(label: "com.mannachurch.WidgetDesignPersistence", qos: .userInitiated)
   ```
4. `onWidgetDesignUpdated`(82~97행 현재 스텁)를 아래로 교체:
   ```swift
   @objc
   func onWidgetDesignUpdated(_ designData: String, resolver resolve: @escaping RCTPromiseResolveBlock, rejecter reject: @escaping RCTPromiseRejectBlock) {
     guard let sharedDefaults = Self.appGroupDefaults(), let containerURL = Self.appGroupContainerURL() else {
       reject("APP_GROUP_ERROR", "App Group을 찾을 수 없습니다.", nil)
       return
     }
     WidgetUpdateModuleImpl.designQueue.async {
       do {
         let persisted = try WidgetDesignPersistence.persist(designData: designData, containerURL: containerURL, backgroundFileName: Constants.widgetDesignBackgroundFileNameSermon)
         sharedDefaults.set(persisted, forKey: Constants.widgetDesignKey)
         sharedDefaults.synchronize()
         WidgetUpdateModuleImpl.reloadWidgets()
         resolve(true)
       } catch {
         NSLog("onWidgetDesignUpdated error: %@", String(describing: error))
         reject("WIDGET_DESIGN_UPDATE_ERROR", String(describing: error), error)
       }
     }
   }
   ```
5. `onQtWidgetDesignUpdated`(99~112행 현재 스텁)를 동일 구조로 교체 — `Constants.widgetDesignQtKey`/`Constants.widgetDesignBackgroundFileNameQt`, 에러 코드 `QT_WIDGET_DESIGN_UPDATE_ERROR`(Android `QT_WIDGET_DESIGN_UPDATE_ERROR`와 동일 네이밍).

`WidgetUpdateModule.mm` 변경 없음(메서드 시그니처 그대로).

- [ ] 구현
- [ ] 커밋: `[ISSUE-220] feat: iOS 위젯 디자인 브릿지가 갤러리 배경을 App Group 컨테이너에 다운샘플링 저장`

---

### Task 3: 단위 테스트 추가 + pbxproj 타깃 연결

**Files:**
- `ios/meditation_blossomTests/WidgetDesignPersistenceTests.swift` (신규)
- `ios/meditation_blossom.xcodeproj/project.pbxproj` (`xcodeproj` gem으로 Sources 빌드 페이즈에 추가)

`WidgetDesignPersistenceTests.swift`:
```swift
import XCTest
import UIKit
@testable import meditation_blossom

final class WidgetDesignPersistenceTests: XCTestCase {
  private var tmpDir: URL!

  override func setUpWithError() throws {
    tmpDir = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
    try FileManager.default.createDirectory(at: tmpDir, withIntermediateDirectories: true)
  }

  override func tearDownWithError() throws {
    try? FileManager.default.removeItem(at: tmpDir)
  }

  func testColorDesignPassesThroughWithoutTouchingFilesystem() throws {
    let json = #"{"text":{"align":"left","color":"#000000","size":16,"weight":"regular"},"background":{"type":"color","value":"gradient-default"}}"#
    let result = try WidgetDesignPersistence.persist(designData: json, containerURL: tmpDir, backgroundFileName: "bg.jpg")
    let decoded = try JSONDecoder().decode(WidgetDesignPersistence.Design.self, from: Data(result.utf8))
    XCTAssertEqual(decoded.background.value, "gradient-default")
    XCTAssertFalse(FileManager.default.fileExists(atPath: tmpDir.appendingPathComponent("bg.jpg").path))
  }

  func testGalleryDesignDownsamplesAndRewritesPathToContainer() throws {
    let sourceURL = tmpDir.appendingPathComponent("source.png")
    try makeSolidColorImage(size: CGSize(width: 2000, height: 1500)).write(to: sourceURL)

    let json = #"{"text":{"align":"center","color":"#FFFFFF","size":20,"weight":"bold"},"background":{"type":"gallery","value":"\#(sourceURL.absoluteString)","imageTransform":{"zoom":1.2,"focalX":0.5,"focalY":0.4}}}"#

    let result = try WidgetDesignPersistence.persist(designData: json, containerURL: tmpDir, backgroundFileName: "bg.jpg")
    let decoded = try JSONDecoder().decode(WidgetDesignPersistence.Design.self, from: Data(result.utf8))

    let expectedPath = tmpDir.appendingPathComponent("bg.jpg").path
    XCTAssertEqual(decoded.background.value, expectedPath)
    XCTAssertTrue(FileManager.default.fileExists(atPath: expectedPath))

    let writtenImage = UIImage(contentsOfFile: expectedPath)
    XCTAssertNotNil(writtenImage)
    XCTAssertLessThanOrEqual(max(writtenImage?.size.width ?? 0, writtenImage?.size.height ?? 0), 1024)
  }

  func testAlreadyPersistedPathIsReReadAndRewritten() throws {
    // 재편집 시 로드된 기존 디자인처럼 스킴 없는 절대 경로를 그대로 다시 저장하는 경우도 지원해야 한다.
    let existingPath = tmpDir.appendingPathComponent("bg.jpg")
    try makeSolidColorImage(size: CGSize(width: 800, height: 800)).write(to: existingPath)

    let json = #"{"text":{"align":"left","color":"#000000","size":16,"weight":"regular"},"background":{"type":"gallery","value":"\#(existingPath.path)"}}"#
    let result = try WidgetDesignPersistence.persist(designData: json, containerURL: tmpDir, backgroundFileName: "bg.jpg")
    let decoded = try JSONDecoder().decode(WidgetDesignPersistence.Design.self, from: Data(result.utf8))
    XCTAssertEqual(decoded.background.value, existingPath.path)
  }

  func testInvalidJSONThrowsInvalidDesignJSON() {
    XCTAssertThrowsError(try WidgetDesignPersistence.persist(designData: "not json", containerURL: tmpDir, backgroundFileName: "bg.jpg")) { error in
      XCTAssertEqual(error as? WidgetDesignPersistence.PersistenceError, .invalidDesignJSON)
    }
  }

  func testUndecodableImageThrowsImageDecodeFailed() throws {
    let sourceURL = tmpDir.appendingPathComponent("not-an-image.png")
    try Data("garbage".utf8).write(to: sourceURL)
    let json = #"{"text":{"align":"left","color":"#000000","size":16,"weight":"regular"},"background":{"type":"gallery","value":"\#(sourceURL.absoluteString)"}}"#

    XCTAssertThrowsError(try WidgetDesignPersistence.persist(designData: json, containerURL: tmpDir, backgroundFileName: "bg.jpg")) { error in
      XCTAssertEqual(error as? WidgetDesignPersistence.PersistenceError, .imageDecodeFailed)
    }
  }

  private func makeSolidColorImage(size: CGSize) throws -> Data {
    let renderer = UIGraphicsImageRenderer(size: size)
    let image = renderer.image { ctx in
      UIColor.systemBlue.setFill()
      ctx.fill(CGRect(origin: .zero, size: size))
    }
    guard let data = image.pngData() else { throw XCTSkip("PNG 인코딩 실패") }
    return data
  }
}
```

(`PersistenceError`가 `Equatable`을 채택해야 `XCTAssertEqual(error as? ...)`이 동작 — Task 1의 enum 선언에 `: Error, Equatable` 추가.)

**pbxproj 연결** (Ruby, `xcodeproj` gem — CocoaPods 의존성으로 이미 설치돼 있음):
```ruby
require 'xcodeproj'
proj = Xcodeproj::Project.open('ios/meditation_blossom.xcodeproj')
group = proj.main_group.find_subpath('meditation_blossomTests', true)
file_ref = group.new_reference('meditation_blossomTests/WidgetDesignPersistenceTests.swift')
target = proj.targets.find { |t| t.name == 'meditation_blossomTests' }
target.source_build_phase.add_file_reference(file_ref)
proj.save
```
(정확한 group 경로/기존 파일 참조 방식은 `meditation_blossomTests` 그룹의 현재 상태를 보고 맞춘다 — 실행 전 `git diff`로 의도치 않은 pbxproj 변경이 없는지 확인.)

- [ ] 구현
- [ ] `cd ios && xcodebuild test -workspace meditation_blossom.xcworkspace -scheme meditation_blossom -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:meditation_blossomTests/WidgetDesignPersistenceTests`
- [ ] 커밋: `[ISSUE-220] test: WidgetDesignPersistence 단위 테스트 추가`

---

### Task 4: 전체 빌드 검증

- [ ] `cd ios && xcodebuild build -workspace meditation_blossom.xcworkspace -scheme meditation_blossom -destination 'platform=iOS Simulator,name=iPhone 16'` — 앱 타깃 컴파일 확인
- [ ] `cd ios && xcodebuild test -workspace meditation_blossom.xcworkspace -scheme meditation_blossom -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:meditation_blossomTests/WidgetDesignPersistenceTests` — 신규 테스트 통과 확인
- [ ] 수동 확인(시뮬레이터, 실제 App Group 컨테이너 대상): RN `EditScreen`에서 갤러리 배경 선택 후 저장 → `onWidgetDesignUpdated` 호출 → 시뮬레이터 App Group 컨테이너 파일 시스템에 `widget_design_background_sermon.jpg`가 생성되는지, `UserDefaults(suiteName:)`에 저장된 JSON의 `background.value`가 그 경로를 가리키는지 확인(위젯 자체 렌더링 반영은 #221 스코프라 육안 확인은 안 됨 — 현재 하드코딩 렌더링 유지).

- [ ] 커밋 없음(검증 전용) — 실패 시 해당 Task로 돌아가 수정 후 재커밋

---

## Self-Review 결과

- **이슈 커버리지**: #220 본문의 두 항목 — "기존 콘텐츠 동기화 메서드 패턴을 참고해 디자인 값 저장 메서드 추가"(Task 2, `onSermonUpdated`/`onQtUpdated`와 동일한 App Group `UserDefaults` + `reloadWidgets` 패턴 재사용), "배경 갤러리 이미지: App Group 컨테이너 파일 경로에 저장 + 리사이즈/압축"(Task 1의 `downsampleAndStore`), "저장/조회 관련 순수 로직 단위 테스트"(Task 3) 모두 다룸.
- **스코프 경계**: `MeditationBlossomWidget.swift` 렌더링 반영은 명시적으로 제외(#221). `WidgetUpdateModule.mm`/TurboModule spec은 메서드 시그니처가 그대로라 변경 대상에서 뺌.
- **기존 컨벤션 재사용**: Android `WidgetDesignPrefsDataSource`의 "다운샘플링 후 고정 파일명에 덮어쓰기" 전략, `onSermonUpdated`/`onQtUpdated`의 App Group `UserDefaults` + `synchronize()` + `reloadWidgets()` 패턴, `resolveBibleReferences`의 전용 `DispatchQueue` 패턴, Android 브릿지의 에러 코드 네이밍(`WIDGET_DESIGN_UPDATE_ERROR`/`QT_WIDGET_DESIGN_UPDATE_ERROR`)을 그대로 iOS에 대칭 적용 — 새 개념을 만들지 않음.
- **리스크**: (1) `xcodeproj` gem으로 pbxproj를 프로그래밍적으로 수정하는 부분은 기존 group 구조에 따라 스크립트를 조정해야 할 수 있음 — Task 3 실행 시 `git diff ios/meditation_blossom.xcodeproj/project.pbxproj`로 의도한 파일 참조 1건만 추가됐는지 반드시 확인. (2) `react-native-image-picker`가 반환하는 URI가 항상 `file://` 스킴이라고 가정(`resolveFileURL`) — 만약 `ph://` 등 다른 스킴을 반환하는 설정이라면 `CGImageSourceCreateWithURL`이 실패해 `imageDecodeFailed`를 던짐; Task 4 수동 확인에서 실제 피커 산출물로 검증 필요.
